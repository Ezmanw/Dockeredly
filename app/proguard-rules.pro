# Keep Room entities/DAOs generated code
-keep class com.dockeredly.app.data.database.** { *; }

# GeckoView / WebView JS interfaces are not used with @JavascriptInterface reflection,
# so no additional keep rules are required for them.
