<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" />

    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="mailto" />
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL" />
            <data android:scheme="tel" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
    </queries>

    <application
        android:name=".DockeredlyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Dockeredly"
        android:enableOnBackInvokedCallback="true"
        android:hardwareAccelerated="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Dockeredly"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Eight identical runtime activity slots, each pinned to its own process via
          android:process. A web app is launched into the slot its profile id hashes to
          (see WebAppLauncher / BrowserProcessSlots), giving Chromium/WebView web apps
          real, OS-enforced storage isolation despite WebView only supporting one active
          data-directory suffix per process.
        -->
        <activity
            android:name=".runtime.WebAppRuntimeActivitySlot0"
            android:process=":webapp0"
            android:exported="false"
            android:theme="@style/Theme.Dockeredly.Runtime"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTask"
            android:resizeableActivity="true" />
        <activity
            android:name=".runtime.WebAppRuntimeActivitySlot1"
            android:process=":webapp1"
            android:exported="false"
            android:theme="@style/Theme.Dockeredly.Runtime"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTask"
            android:resizeableActivity="true" />
        <activity
            android:name=".runtime.WebAppRuntimeActivitySlot2"
            android:process=":webapp2"
            android:exported="false"
            android:theme="@style/Theme.Dockeredly.Runtime"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTask"
            android:resizeableActivity="true" />
        <activity
            android:name=".runtime.WebAppRuntimeActivitySlot3"
            android:process=":webapp3"
            android:exported="false"
            android:theme="@style/Theme.Dockeredly.Runtime"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTask"
            android:resizeableActivity="true" />
        <activity
            android:name=".runtime.WebAppRuntimeActivitySlot4"
            android:process=":webapp4"
            android:exported="false"
            android:theme="@style/Theme.Dockeredly.Runtime"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTask"
            android:resizeableActivity="true" />
        <activity
            android:name=".runtime.WebAppRuntimeActivitySlot5"
            android:process=":webapp5"
            android:exported="false"
            android:theme="@style/Theme.Dockeredly.Runtime"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTask"
            android:resizeableActivity="true" />
        <activity
            android:name=".runtime.WebAppRuntimeActivitySlot6"
            android:process=":webapp6"
            android:exported="false"
            android:theme="@style/Theme.Dockeredly.Runtime"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTask"
            android:resizeableActivity="true" />
        <activity
            android:name=".runtime.WebAppRuntimeActivitySlot7"
            android:process=":webapp7"
            android:exported="false"
            android:theme="@style/Theme.Dockeredly.Runtime"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTask"
            android:resizeableActivity="true" />

    </application>

</manifest>
